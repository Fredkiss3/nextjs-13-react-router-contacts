import * as React from "react";
import { EditForm } from "~/app/(components)/edit-form";

export type NewContactProps = {};

export default function NewContact({}: NewContactProps) {
    return <EditForm />;
}
